# job for generating site for master branch for gitlab pages
pages:
  stage: deploy
  image: ruby:latest
  before_script:
    - bundle install
  script:
    - bundle exec jekyll build -d public
  artifacts:
    paths:
      - public
  only:
    - master